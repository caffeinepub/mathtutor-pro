{
  "frontend/src/pages/PaymentSuccess.tsx": "Payment success page updated to use valid Session status `'scheduled'` instead of `'confirmed'`, and creates notifications using the new Notification interface (no `userId`/`read` fields).",
  "frontend/src/layouts/AdminLayout.tsx": "Admin portal layout using `<Outlet />` for child routes (no children prop), with collapsible sidebar including all nav items (Dashboard, Students, Courses, Sessions, Manage Student Sessions, Manage Student Materials, Attendance Management, Payments, Notifications), pending payment badge, and logout.",
  "frontend/src/layouts/StudentLayout.tsx": "Student portal layout using `<Outlet />` for child routes (no children prop), with sidebar including My Sessions and My Materials nav items, notification bell, and logout; reads student from local store via auth userId.",
  "frontend/src/pages/Login.tsx": "Login page with email/password form and role selector; now calls `storeAuthState()` from `auth.ts` to persist auth under the `mathtutor_auth` key that route guards read, fixing the blank admin dashboard after login. Also keeps legacy `currentUser`/`currentStudent` keys for backward compatibility.",
  "frontend/src/pages/PaymentFailure.tsx": "Payment failure page with retry and dashboard links; fixes `/student/` → `/student` Link path.",
  "frontend/src/pages/admin/Sessions.tsx": "Admin sessions management page updated to use valid Session statuses (`scheduled`, `completed`, `cancelled`), removed references to `duration`/`price`/`type` fields, and uses new Notification interface without `userId`/`read`.",
  "frontend/src/pages/admin/Courses.tsx": "Admin courses management page with create/edit modal, active toggle, delete confirmation, and a WhatsApp demo inquiry button on each course card.",
  "frontend/src/pages/admin/Materials.tsx": "Admin materials page updated to use strictly-typed `Material['fileType']` values (`pdf`, `video`, `image`, `doc`, `other`) and new Notification interface with `targetStudentId`/`readBy` fields.",
  "frontend/src/pages/admin/Notifications.tsx": "Admin notifications page updated to use the new Notification interface with `readBy`/`targetStudentId` instead of `userId`/`read`; broadcasts use no `targetStudentId`, specific sends use `targetStudentId`.",
  "frontend/src/pages/student/Dashboard.tsx": "Student dashboard with welcome banner, attendance summary stats, upcoming sessions with Join buttons, and recent materials — uses `AttendanceStatus` enum for comparisons and safe `ArrayBuffer` cast for file downloads.",
  "frontend/src/pages/student/Courses.tsx": "Student enrolled courses page displaying course cards with pricing, materials link, and a WhatsApp demo booking button per course; shows empty state with WhatsApp contact button when no courses are enrolled.",
  "frontend/src/pages/student/Materials.tsx": "Student study materials page filtered to enrolled courses, grouped by course, with file type badges and a course filter dropdown.",
  "frontend/src/pages/student/Sessions.tsx": "Student sessions page showing upcoming and past sessions in tabs, rewritten to use `getStore()` directly instead of removed store accessor methods (`getByUserId`, `getByStudentId`, `getById`), with WhatsApp booking CTA on empty state.",
  "frontend/src/pages/student/Payments.tsx": "Student payments page updated to use new Payment interface fields (`courseName`, `sessionType`, `hours`, `pricePerHour`) instead of `sessionId`/`method`; uses `getAuthState()` from store for auth.",
  "frontend/src/pages/student/Book.tsx": "Student session booking page with 3-step flow (course selection with WhatsApp CTA per course, date/time picker, confirmation); fixes the `studentId` type error by guarding against undefined student and using a properly typed `Session` object with `saveStore`.",
  "frontend/src/pages/student/Profile.tsx": "Student profile page displaying personal info, account status, and a prominent access code card with copy-to-clipboard for approved students.",
  "frontend/src/routeTree.ts": "TanStack Router route tree with all public, admin, and student routes; uses proper ES imports (no require()), exports only `routeTree`, and applies role-based beforeLoad guards on layout routes only.",
  "frontend/src/index.css": "Global stylesheet with Tailwind directives, Inter font import, OKLCH design tokens for light/dark modes, and base body styles — placed in `src/` where `main.tsx` expects it.",
  "frontend/tailwind.config.js": "Tailwind configuration extended with Inter font family, all OKLCH semantic color tokens, and explicit `navy` and `gold` brand color values used throughout the app's components.",
  "frontend/src/components/WhatsAppButton.tsx": "Reusable WhatsApp CTA button component styled in WhatsApp green (#25D366) that opens a pre-filled wa.me link to 919424135055 in a new tab, accepting optional className, message, and label props.",
  "frontend/src/pages/Register.tsx": "Student registration page updated to create student records matching the new Student interface with required `course`, `sessionType`, `accessCode`, and `registeredAt` fields; also adds course and session type selection to the form.",
  "frontend/src/pages/Landing.tsx": "Public landing page with navbar showing the Rajat's Equation logo, hero section, stats bar, courses grid with WhatsApp demo booking buttons per course, features section, testimonials, CTA section, and footer with attribution.",
  "frontend/src/lib/store.ts": "localStorage-based data store with corrected admin credentials (`admin@mathtutor.com` / `Admin@123`); `getStore()` now always replaces any stale admin record with the canonical `defaultAdmin` on every read, making credential migration idempotent across fresh and existing deploys.",
  "frontend/index.html": "HTML entry point with updated title \"The Rajat's Equation\", favicon pointing to the generated logo asset, and apple-touch-icon meta tag.",
  "frontend/src/layouts/RootLayout.tsx": "Root layout that calls `getStore()` on mount to initialize/migrate the store (including admin credential sync) before any child routes render.",
  "frontend/src/components/NotificationBell.tsx": "Notification bell popover component updated to use the new Notification interface with `readBy: string[]` array and `targetStudentId` instead of `userId`/`read` fields; accepts `studentId` prop and filters/marks notifications accordingly.",
  "frontend/src/components/ReceiptModal.tsx": "Payment receipt modal updated to use the new Payment interface fields (`courseName`, `sessionType`, `hours`, `pricePerHour`, `upiTransactionId`, `studentName`) instead of removed `sessionId`/`method` fields.",
  "frontend/src/pages/admin/Dashboard.tsx": "Admin dashboard updated to use `registeredAt` instead of `createdAt` for student sorting, and `'scheduled'` instead of `'confirmed'`/`'pending'` for session status filtering.",
  "frontend/src/pages/admin/Payments.tsx": "Admin payments page updated to use new Payment interface fields (`courseName`, `sessionType`, `hours`, `upiTransactionId`, `studentName`) instead of `sessionId`/`method`; adds approve/reject actions for pending payments.",
  "frontend/src/hooks/useSessions.ts": "React Query hooks for session management — useGetSessionsForStudent (query), useAddSession (mutation), and useDeleteSession (mutation) — with proper query invalidation.",
  "frontend/src/hooks/useMaterials.ts": "React Query hooks for material management — useGetMaterialsForStudent (query), useAddMaterial (mutation), and useDeleteMaterial (mutation) — with proper query invalidation.",
  "frontend/src/hooks/useAttendance.ts": "React Query hooks for attendance management — useGetAttendanceForStudent (query), useGetAttendanceSummary (query), and useMarkAttendance (mutation) — with proper query invalidation.",
  "frontend/src/pages/admin/ManageStudentSessions.tsx": "Admin page for managing per-student sessions; includes student dropdown (approved students from getAllPayments), add session form (date/time/duration/meet link/topic), and sessions table with delete action.",
  "frontend/src/pages/admin/ManageStudentMaterials.tsx": "Admin page for managing per-student materials; includes student dropdown, URL/file upload mode toggle, add material form, and materials table with delete action.",
  "frontend/src/pages/admin/AttendanceManagement.tsx": "Admin page for marking and viewing student attendance; uses `AttendanceStatus` enum (not `__kind__`) for Present/Absent comparisons in the attendance records table.",
  "frontend/src/pages/student/MySessions.tsx": "Dedicated student page listing all assigned sessions sorted by date, with attendance status badges using `AttendanceStatus` enum comparison and a large Join Session on Google Meet button per session.",
  "frontend/src/pages/student/MyMaterials.tsx": "Dedicated student page listing all assigned materials with view/download buttons; uses `Material` type from backend and safe `new Uint8Array(fileData.buffer as ArrayBuffer)` cast for Blob construction."
}