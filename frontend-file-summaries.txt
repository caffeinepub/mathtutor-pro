{
  "frontend/src/pages/PaymentSuccess.tsx": "Payment success page updated to use valid Session status `'scheduled'` instead of `'confirmed'`, and creates notifications using the new Notification interface (no `userId`/`read` fields).",
  "frontend/src/layouts/AdminLayout.tsx": "Admin portal layout with collapsible sidebar, all nav items, pending payment badge, logout, and a new `HealthBadge` component in the sidebar that shows Online/Service Unavailable status using `useCanisterHealth` with a tooltip showing last check time.",
  "frontend/src/layouts/StudentLayout.tsx": "Student portal layout with sidebar navigation that calls Internet Identity clear() AND clearAuthState() on logout, shows the II principal in the user section, and redirects to /login if no valid auth state is found.",
  "frontend/src/pages/Login.tsx": "Login page with Internet Identity button for students (no email/code fields) that checks isCallerApproved() after II login and redirects approved students to the dashboard or shows a pending message; admin login section (email + password for mrjain950761@gmail.com) is completely unchanged.",
  "frontend/src/pages/PaymentFailure.tsx": "Payment failure page with retry and dashboard links; fixes `/student/` → `/student` Link path.",
  "frontend/src/pages/admin/Sessions.tsx": "Admin sessions management page updated to use valid Session statuses (`scheduled`, `completed`, `cancelled`), removed references to `duration`/`price`/`type` fields, and uses new Notification interface without `userId`/`read`.",
  "frontend/src/pages/admin/Courses.tsx": "Admin courses management page with create/edit modal, active toggle, delete confirmation, and a WhatsApp demo inquiry button on each course card.",
  "frontend/src/pages/admin/Materials.tsx": "Admin materials page updated to use strictly-typed `Material['fileType']` values (`pdf`, `video`, `image`, `doc`, `other`) and new Notification interface with `targetStudentId`/`readBy` fields.",
  "frontend/src/pages/admin/Notifications.tsx": "Admin notifications page updated to use the new Notification interface with `readBy`/`targetStudentId` instead of `userId`/`read`; broadcasts use no `targetStudentId`, specific sends use `targetStudentId`.",
  "frontend/src/pages/student/Dashboard.tsx": "Student dashboard showing welcome banner, stats (total/upcoming sessions, materials count), upcoming classes with join buttons, and recent materials — all null-safe with proper auth checks.",
  "frontend/src/pages/student/Courses.tsx": "Student enrolled courses page showing course cards with pricing and WhatsApp CTA; fixes null auth guard by using store.students.find with null-safe auth check.",
  "frontend/src/pages/student/Materials.tsx": "Student materials page filtered to the logged-in student's ID from localStorage with null-safe auth checks, grouped by course, with view buttons for each material.",
  "frontend/src/pages/student/Sessions.tsx": "Student sessions page that null-checks auth before filtering, separates sessions into upcoming and past sections, and renders join buttons only when meetLink is present.",
  "frontend/src/pages/student/Payments.tsx": "Student payments history page with stats, payment cards, and receipt modal; uses only valid Payment status values (no 'failed'), null-safe date handling via createdAt only, and safe amount calculation.",
  "frontend/src/pages/student/Book.tsx": "Student session booking page with course selection, session type radio cards, a number input for hours (1–1000), optional preferred date/time and notes, UPI payment section; creates a properly-typed Payment record (with required `amount` field) in localStorage on submit.",
  "frontend/src/pages/student/Profile.tsx": "Student profile page showing personal info, access code with copy button, and enrolled courses; fixes null auth guard by using safe auth check with store lookup.",
  "frontend/src/routeTree.ts": "TanStack Router route tree with all public, admin, and student routes using direct (non-lazy) imports; fixes the auth key mismatch by reading from `'rajats_equation_auth'` (matching store.ts) instead of the old `'mathtutor_auth'` key, so role-based beforeLoad guards correctly allow admin and student navigation after login.",
  "frontend/src/index.css": "Global stylesheet with Tailwind directives, Inter font import, OKLCH design tokens for light/dark modes, and base body styles — placed in `src/` where `main.tsx` expects it.",
  "frontend/tailwind.config.js": "Tailwind configuration extended with Inter font family, all OKLCH semantic color tokens, and explicit `navy` and `gold` brand color values used throughout the app's components.",
  "frontend/src/components/WhatsAppButton.tsx": "Reusable WhatsApp CTA button component styled in WhatsApp green (#25D366) that opens a pre-filled wa.me link to 919424135055 in a new tab, accepting optional className, message, and label props.",
  "frontend/src/pages/Register.tsx": "Student registration page with personal info, course selection dropdown, session type radio cards, a number input for hours (1–1000), UPI QR code display, and a success screen; creates properly-typed Payment and Student records in localStorage using only valid Student fields (`course` not `courseName`).",
  "frontend/src/pages/Landing.tsx": "Public landing page with navbar, hero, stats, courses grid, features, testimonials, CTA, and footer; wraps getStore() in a try-catch so a store failure never crashes the page.",
  "frontend/src/lib/store.ts": "localStorage-based data store with fully typed interfaces; adds `uniqueCode` optional field to both `Student` and `Payment` interfaces, updates `findStudentByEmailAndCode` to match on either `accessCode` or `uniqueCode`, and updates `updateStudentStatus` to accept and persist an optional `uniqueCode` parameter.",
  "frontend/index.html": "HTML entry point with updated title \"The Rajat's Equation\", favicon pointing to the generated logo asset, and apple-touch-icon meta tag.",
  "frontend/src/layouts/RootLayout.tsx": "Root layout that calls initializeStore() on mount (only seeds defaults if store is empty) with all errors silently caught.",
  "frontend/src/components/NotificationBell.tsx": "Notification bell popover component updated to use the new Notification interface with `readBy: string[]` array and `targetStudentId` instead of `userId`/`read` fields; accepts `studentId` prop and filters/marks notifications accordingly.",
  "frontend/src/components/ReceiptModal.tsx": "Payment receipt modal updated to use the new Payment interface fields (`courseName`, `sessionType`, `hours`, `pricePerHour`, `upiTransactionId`, `studentName`) instead of removed `sessionId`/`method` fields.",
  "frontend/src/pages/admin/Dashboard.tsx": "Admin dashboard showing real stats (total/active/pending students, sessions, materials, approved payments) read from localStorage, plus recent students and sessions lists.",
  "frontend/src/pages/admin/Payments.tsx": "Admin payments management page with approve/reject actions and success modal showing generated credentials; now includes IC0508 error detection with a user-friendly error banner, and a `showError` helper that distinguishes canister-stopped errors from generic failures.",
  "frontend/src/hooks/useSessions.ts": "React Query hooks for session management — useGetSessionsForStudent (query), useAddSession (mutation), and useDeleteSession (mutation) — with proper query invalidation.",
  "frontend/src/hooks/useMaterials.ts": "React Query hooks for material management — useGetMaterialsForStudent (query), useAddMaterial (mutation), and useDeleteMaterial (mutation) — with proper query invalidation.",
  "frontend/src/hooks/useAttendance.ts": "React Query hooks for attendance management — useGetAttendanceForStudent (query), useGetAttendanceSummary (query), and useMarkAttendance (mutation) — with proper query invalidation.",
  "frontend/src/pages/admin/ManageStudentSessions.tsx": "Admin sessions (classes) management page with a two-step flow — select an approved student, then add a class with date/time/duration/Google Meet link/optional topic — all stored in localStorage and displayed in a per-student sessions list with delete action.",
  "frontend/src/pages/admin/ManageStudentMaterials.tsx": "Admin materials management page mirroring the sessions structure — select an approved student, then add a material with title/file type/URL/course/description — stored per-student in localStorage with a materials list and delete action.",
  "frontend/src/pages/admin/AttendanceManagement.tsx": "Admin attendance management page that uses Principal (not bigint) for backend hook calls; selects students from approved UPI payments, shows sessions and attendance records, with a graceful fallback message for principal-based marking.",
  "frontend/src/pages/student/MySessions.tsx": "Student my-sessions page listing all assigned sessions sorted by date with upcoming/past badges and conditional join buttons, with null-safe auth checks.",
  "frontend/src/pages/student/MyMaterials.tsx": "Student my-materials page listing all assigned materials sorted by upload date with null-safe auth checks and view buttons.",
  "frontend/src/App.tsx": "Root application component with QueryClientProvider, RouterProvider using the routeTree, and a React ErrorBoundary class component to catch and display initialization errors gracefully instead of crashing.",
  "frontend/src/main.tsx": "Application entry point that mounts the React app into the root DOM element with StrictMode and no additional providers (QueryClient and Router are in App.tsx).",
  "frontend/src/lib/auth.ts": "Thin localStorage auth helper with AuthState interface (now including optional `email` field for backward compatibility), storeAuthState/getAuthState/clearAuthState helpers, and getCurrentStudentId/isAdmin/isStudent utilities; student auth relies on Internet Identity principal stored after II login + approval check.",
  "frontend/src/pages/admin/Students.tsx": "Admin students management page with approve/reject/revoke actions; now wraps all backend mutation errors with IC0508 detection — showing a user-friendly \"backend temporarily unavailable\" message instead of raw error text, with a spinner on the approve button during pending state.",
  "frontend/src/hooks/useCanisterHealth.ts": "New custom hook that performs a lightweight backend health check every 60 seconds using `getProducts()`, returning `{ isOnline, isChecking, lastChecked }` — detects IC0508 \"canister stopped\" errors and sets `isOnline` to false accordingly."
}