{
  "frontend/src/pages/PaymentSuccess.tsx": "Payment success page updated to use valid Session status `'scheduled'` instead of `'confirmed'`, and creates notifications using the new Notification interface (no `userId`/`read` fields).",
  "frontend/src/layouts/AdminLayout.tsx": "Admin portal layout that now uses the `isPending` state from `useCanisterHealth` to show a loading screen during the initial connection probe, only rendering the `BackendUnavailableScreen` error after the health check has definitively returned offline.",
  "frontend/src/layouts/StudentLayout.tsx": "Student portal layout that now uses the `isPending` state from `useCanisterHealth` to suppress the offline banner during the initial connection probe, only showing the `OfflineBanner` and \"Cached\" indicator after the health check has definitively returned offline.",
  "frontend/src/pages/Login.tsx": "Simplified student/admin login page using Internet Identity only — no access code or payment gate; students click \"Sign in with Internet Identity\" and are immediately redirected to the student portal, while admins use the same flow to reach the admin dashboard.",
  "frontend/src/pages/PaymentFailure.tsx": "Payment failure page with retry and dashboard links; fixes `/student/` → `/student` Link path.",
  "frontend/src/pages/admin/Sessions.tsx": "Admin sessions management page updated to use valid Session statuses (`scheduled`, `completed`, `cancelled`), removed references to `duration`/`price`/`type` fields, and uses new Notification interface without `userId`/`read`.",
  "frontend/src/pages/admin/Courses.tsx": "Admin courses management page with create/edit modal, active toggle, delete confirmation, and a WhatsApp demo inquiry button on each course card.",
  "frontend/src/pages/admin/Materials.tsx": "Admin materials page updated to use strictly-typed `Material['fileType']` values (`pdf`, `video`, `image`, `doc`, `other`) and new Notification interface with `targetStudentId`/`readBy` fields.",
  "frontend/src/pages/admin/Notifications.tsx": "Admin notifications page updated to use the new Notification interface with `readBy`/`targetStudentId` instead of `userId`/`read`; broadcasts use no `targetStudentId`, specific sends use `targetStudentId`.",
  "frontend/src/pages/student/Dashboard.tsx": "Student dashboard that fetches the authenticated student's enrollment via useMyEnrollment and upcoming sessions via useMyStudentSessions, showing enrolled course details, upcoming sessions with Join Meeting buttons, and a prompt to register if no enrollment exists.",
  "frontend/src/pages/student/Courses.tsx": "Student courses page showing enrolled course from backend and available courses catalog; fixes `__kind` → `__kind__` discriminant accesses on Student paymentStatus and extracts pending check into a type-safe helper function.",
  "frontend/src/pages/student/Materials.tsx": "Student materials page reading from localStorage store, grouped by course, with offline indicator badge, view links, and graceful empty state.",
  "frontend/src/pages/student/Sessions.tsx": "Student sessions page fetching sessions from the backend via useMyStudentSessions, separated into upcoming and past sections with Join Meeting buttons for Google Meet links and empty state handling.",
  "frontend/src/pages/student/Payments.tsx": "Student payments page importing `getAuthState` from `auth.ts`, using `auth.studentId`, and only valid `Payment` fields (`amount`, status values `'pending'|'approved'|'rejected'`).",
  "frontend/src/pages/student/Book.tsx": "Student session booking page with course selection, session type, hours, and UPI payment — now generates a dynamic UPI QR code via api.qrserver.com encoding the correct `upi://pay?pa=9424135055@ptyes&pn=Rajat's Equation&am=...&cu=INR` deep link with error correction level H, plus a fallback section with copyable UPI ID (`9424135055@ptyes`), copy payment link button, and a direct \"Open Payment App\" deep link anchor.",
  "frontend/src/pages/student/Profile.tsx": "Student profile page reading from localStorage store using only valid Student fields (createdAt instead of joinedAt), looking up pricePerHour from the matched Course record instead of Student, with offline indicator badge.",
  "frontend/src/routeTree.ts": "TanStack Router route tree where the student route guard only checks that any auth role exists (student or admin) — no payment or approval status check — while admin routes still require the admin role.",
  "frontend/src/index.css": "Global stylesheet with Tailwind directives, Inter font import, OKLCH design tokens for light/dark modes, and base body styles — placed in `src/` where `main.tsx` expects it.",
  "frontend/tailwind.config.js": "Tailwind configuration extended with Inter font family, all OKLCH semantic color tokens, and explicit `navy` and `gold` brand color values used throughout the app's components.",
  "frontend/src/components/WhatsAppButton.tsx": "Reusable WhatsApp CTA button component styled in WhatsApp green (#25D366) that opens a pre-filled wa.me link to 919424135055 in a new tab, accepting optional className, message, and label props.",
  "frontend/src/pages/Register.tsx": "Student registration page with three-step flow; fixes `__kind` → `__kind__` discriminant access on PaymentResult and uses correct `__kind__` when constructing the paymentType argument for finishStudentRegistration.",
  "frontend/src/pages/Landing.tsx": "Public landing page with navbar, hero, stats, courses grid, features, testimonials, CTA, and footer; wraps getStore() in a try-catch so a store failure never crashes the page.",
  "frontend/src/lib/store.ts": "localStorage-based data store with fully typed interfaces; adds `uniqueCode` optional field to both `Student` and `Payment` interfaces, updates `findStudentByEmailAndCode` to match on either `accessCode` or `uniqueCode`, and updates `updateStudentStatus` to accept and persist an optional `uniqueCode` parameter.",
  "frontend/index.html": "HTML entry point with updated title \"The Rajat's Equation\", favicon pointing to the generated logo asset, and apple-touch-icon meta tag.",
  "frontend/src/layouts/RootLayout.tsx": "Root layout that calls initializeStore() on mount (only seeds defaults if store is empty) with all errors silently caught.",
  "frontend/src/components/NotificationBell.tsx": "Notification bell popover component updated to use the new Notification interface with `readBy: string[]` array and `targetStudentId` instead of `userId`/`read` fields; accepts `studentId` prop and filters/marks notifications accordingly.",
  "frontend/src/components/ReceiptModal.tsx": "Payment receipt modal updated to use the new Payment interface fields (`courseName`, `sessionType`, `hours`, `pricePerHour`, `upiTransactionId`, `studentName`) instead of removed `sessionId`/`method` fields.",
  "frontend/src/pages/admin/Dashboard.tsx": "Admin dashboard with real-time stats from backend; fixes all `__kind` → `__kind__` discriminant accesses on Student paymentStatus, UpiPaymentStatus, StripeSessionStatus, and UpiPayment status fields.",
  "frontend/src/pages/admin/Payments.tsx": "Admin payments management page updated to import `useApprovePayment` and `useRejectPayment` (canonical names) instead of the old `useApproveUpiPayment`/`useRejectUpiPayment`; all approve/reject/view dialogs and payment list remain unchanged.",
  "frontend/src/hooks/useSessions.ts": "React Query hooks for session management accepting Principal objects — useGetSessionsForStudent (query with null guard), useAddSession (mutation), and useDeleteSession (mutation) with proper cache invalidation.",
  "frontend/src/hooks/useMaterials.ts": "React Query hooks for material management accepting Principal objects — useGetMaterialsForStudent (query with null guard), useAddMaterial (mutation), and useDeleteMaterial (mutation) with proper cache invalidation.",
  "frontend/src/hooks/useAttendance.ts": "React Query hooks for attendance management — useGetAttendanceForStudent (query), useGetAttendanceSummary (query), and useMarkAttendance (mutation) — with proper query invalidation.",
  "frontend/src/pages/admin/ManageStudentSessions.tsx": "Admin session management page updated to show ALL registered students (not just active ones) in the student selector dropdown and reference panel, with a search filter on the reference panel, per-student session list, and add/delete session functionality that saves sessions only to the selected student.",
  "frontend/src/pages/admin/ManageStudentMaterials.tsx": "Fully rewritten admin materials page using `useAllStudents` to populate the student selector with all registered students; fixes TypeScript errors by passing `null` instead of `undefined` for `description`, `fileData`, and `fileLink` fields to match the `useMaterials` hook's expected `string | null` and `Uint8Array | null` types.",
  "frontend/src/pages/admin/AttendanceManagement.tsx": "Admin attendance management page that uses Principal (not bigint) for backend hook calls; selects students from approved UPI payments, shows sessions and attendance records, with a graceful fallback message for principal-based marking.",
  "frontend/src/pages/student/MySessions.tsx": "Student my-classes page fetching sessions from the backend via useMyStudentSessions, sorted by date with upcoming/past badges and Join Meeting buttons for Google Meet links.",
  "frontend/src/pages/student/MyMaterials.tsx": "Student my-materials page reading from localStorage store sorted by date, with offline indicator badge, view links, and graceful empty state.",
  "frontend/src/App.tsx": "Root application component with QueryClientProvider (retry:1, staleTime:30s), RouterProvider, Toaster (richColors, top-right), and an ErrorBoundary class component for graceful crash handling.",
  "frontend/src/main.tsx": "Application entry point that mounts the React app into the root DOM element with StrictMode and no additional providers (QueryClient and Router are in App.tsx).",
  "frontend/src/lib/auth.ts": "Thin localStorage auth helper with AuthState interface (including optional email/name/uniqueCode fields), storeAuthState/getAuthState/clearAuthState helpers, and new offline credential caching functions (cacheStudentCredentials, getCachedCredentials, clearCachedCredentials, validateCachedCredentials, hasCachedCredentials).",
  "frontend/src/pages/admin/Students.tsx": "Admin students page with full student list fetched from backend, search/filter, View detail modal, and a new Edit modal (`EditStudentModal`) that pre-populates all student fields (name, email, phone, course, sessionType, hours, transactionId, enrollmentDate, paymentStatus, isActive) and saves via `useUpdateStudent` with validation.",
  "frontend/src/hooks/useCanisterHealth.ts": "Custom hook that polls backend health every 30 seconds using getProducts(), now includes an `isPending` state that stays `true` until the first health check completes (or while the actor is still initializing), preventing false-positive offline detection during startup.",
  "frontend/src/hooks/useReconnectionToast.ts": "Hook that monitors useCanisterHealth for backend recovery events (offline→online), shows a sonner toast notification on recovery, and invalidates all React Query caches to refresh stale data.",
  "frontend/src/components/OfflineBanner.tsx": "Non-blocking amber banner component that displays a WifiOff icon and cached-data message when isOnline is false; renders nothing when online.",
  "frontend/src/components/BackendUnavailableScreen.tsx": "Full-screen component shown to admin users when the backend is offline; now accepts an `isPending` prop that renders a neutral loading spinner instead of the error screen while the initial health check is still in progress.",
  "frontend/src/hooks/useQueries.ts": "Complete React Query hooks file with all backend operations including a new `useUpdateStudent` mutation that calls `actor.updateStudent` with all student fields and invalidates the `allStudents` and `myEnrollment` query keys on success."
}